/**
 * <`0:name`>
 *
 * @param {} word - <`0:description`>
 */
export const A = (word) => {
  console.log(word);
  return word;
};

/**
 * It's for the test to use vim-jsdoc and vim keymapping.
 *
 * @param {string} word - <`0:description`>
 * @param {string} word2 - <`1:description`>
 * @return {string} 
 */
const B = (word, word2) => {
  console.log(word);
  return word + word2;
};
/**
 * A song
 * @typedef  Song
 */
/**
 * Plays a song
 * @param {Song} song - The {@link Song} to be played
 */

function play (song) {
}
